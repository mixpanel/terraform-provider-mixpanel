# Terraform state & local config (may contain secrets / resource data)
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform/
.terraform.lock.hcl
.terraformrc
terraform.rc
crash.log
crash.*.log

# Credentials / local env
.env
.env.*

# Built provider binary & release artifacts
terraform-provider-mixpanel
terraform-provider-mixpanel_*
dist/
*.exe

# Generator: transient full spec (the committed input is gen/spec/openapi.pruned.json)
# and stage-1 build scratch. See gen/README.md.
gen/spec/openapi.merged.json
gen/build/
