// Code generated by gen/crudgen.py. DO NOT EDIT.

package provider

import (
	"context"
	"fmt"
	"strings"

	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/resource/schema"
	"github.com/hashicorp/terraform-plugin-framework/tfsdk"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-go/tftypes"

	"github.com/mixpanel/terraform-provider-mixpanel/internal/client"
	rsc "github.com/mixpanel/terraform-provider-mixpanel/internal/provider/resource_rollup_project"
)

var (
	_ resource.Resource                = (*RollupProjectResource)(nil)
	_ resource.ResourceWithConfigure   = (*RollupProjectResource)(nil)
	_ resource.ResourceWithImportState = (*RollupProjectResource)(nil)
)

// keep the generated schema package and schema builder imported.
var _ = schema.StringAttribute{}

// keep the types package imported (used by nested schema overrides).
var _ = types.NumberType

// NewRollupProjectResource constructs the rollup_project resource.
func NewRollupProjectResource() resource.Resource {
	return &RollupProjectResource{}
}

type RollupProjectResource struct {
	client *client.Client
}

func (r *RollupProjectResource) Metadata(ctx context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_rollup_project"
}

func (r *RollupProjectResource) Schema(ctx context.Context, req resource.SchemaRequest, resp *resource.SchemaResponse) {
	s := rsc.RollupProjectResourceSchema(ctx)
	stabilizeComputed(s.Attributes)
	resp.Schema = s
}

func (r *RollupProjectResource) Configure(ctx context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}
	c, ok := req.ProviderData.(*client.Client)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *client.Client, got: %T.", req.ProviderData),
		)
		return
	}
	r.client = c
}

// projectID resolves the project from the rollup_project project_id attribute (if any)
// falling back to the provider default.
func (r *RollupProjectResource) projectID(ctx context.Context, raw tftypes.Value) (string, error) {
	_ = raw
	return "", nil
}

func (r *RollupProjectResource) collectionPath(projectID string) string {
	return strings.NewReplacer("{project_id}", projectID).Replace("/api/app/rollup_projects")
}

func (r *RollupProjectResource) instancePath(projectID, id string) string {
	return strings.NewReplacer("{project_id}", projectID, "{rollup_project_id}", id).Replace("/api/app/rollup_projects/{rollup_project_id}")
}

func (r *RollupProjectResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	spec := RollupProjectAttrSpec()
	projectID, err := r.projectID(ctx, req.Plan.Raw)
	if err != nil {
		resp.Diagnostics.AddError("Resolving project_id", err.Error())
		return
	}
	body, err := client.WireFromRaw(req.Plan.Raw, spec)
	if err != nil {
		resp.Diagnostics.AddError("Encoding rollup_project request", err.Error())
		return
	}
	respBody, err := r.client.Do(ctx, "POST", r.collectionPath(projectID), body)
	if err != nil {
		resp.Diagnostics.AddError("Creating rollup_project", err.Error())
		return
	}
	wire, err := unwrapRollupProject(respBody)
	if err != nil {
		resp.Diagnostics.AddError("Decoding rollup_project response", err.Error())
		return
	}
	id := idForRollupProject(wire)
	r.writeRollupProjectState(ctx, &resp.State, &resp.Diagnostics, req.Plan.Raw, wire, projectID, id)
}

func (r *RollupProjectResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	projectID, err := r.projectID(ctx, req.State.Raw)
	if err != nil {
		resp.Diagnostics.AddError("Resolving project_id", err.Error())
		return
	}
	id, err := stringAttrFromRaw(req.State.Raw, "rollup_project_id")
	if err != nil {
		resp.Diagnostics.AddError("Reading rollup_project id", err.Error())
		return
	}
	respBody, err := r.client.Do(ctx, "GET", r.instancePath(projectID, id), nil)
	if err != nil {
		if apiErr, ok := err.(*client.APIError); ok && apiErr.StatusCode == 404 {
			resp.State.RemoveResource(ctx)
			return
		}
		resp.Diagnostics.AddError("Reading rollup_project", err.Error())
		return
	}
	wire, err := unwrapRollupProject(respBody)
	if err != nil {
		resp.Diagnostics.AddError("Decoding rollup_project response", err.Error())
		return
	}
	// Use the prior state as the merge base so attributes the user manages but
	// the API does not faithfully echo back on a GET (fields it never returns, or
	// returns enriched with server-assigned sub-keys such as a subscription id)
	// are preserved instead of being clobbered to null / a server-mangled shape,
	// which would otherwise produce a permanent post-refresh diff. Computed-only
	// values (absent from prior state) are still refreshed from the API response.
	r.writeRollupProjectState(ctx, &resp.State, &resp.Diagnostics, req.State.Raw, wire, projectID, id)
}

func (r *RollupProjectResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	spec := RollupProjectAttrSpec()
	projectID, err := r.projectID(ctx, req.Plan.Raw)
	if err != nil {
		resp.Diagnostics.AddError("Resolving project_id", err.Error())
		return
	}
	id, err := stringAttrFromRaw(req.State.Raw, "rollup_project_id")
	if err != nil {
		resp.Diagnostics.AddError("Reading rollup_project id", err.Error())
		return
	}
	body, err := client.WireFromRaw(req.Plan.Raw, spec)
	if err != nil {
		resp.Diagnostics.AddError("Encoding rollup_project request", err.Error())
		return
	}
	respBody, err := r.client.Do(ctx, "POST", r.instancePath(projectID, id), body)
	if err != nil {
		resp.Diagnostics.AddError("Updating rollup_project", err.Error())
		return
	}
	wire, err := unwrapRollupProject(respBody)
	if err != nil {
		resp.Diagnostics.AddError("Decoding rollup_project response", err.Error())
		return
	}
	r.writeRollupProjectState(ctx, &resp.State, &resp.Diagnostics, req.Plan.Raw, wire, projectID, id)
}

func (r *RollupProjectResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	projectID, err := r.projectID(ctx, req.State.Raw)
	if err != nil {
		resp.Diagnostics.AddError("Resolving project_id", err.Error())
		return
	}
	id, err := stringAttrFromRaw(req.State.Raw, "rollup_project_id")
	if err != nil {
		resp.Diagnostics.AddError("Reading rollup_project id", err.Error())
		return
	}
	// DELETE may return a JSON body (Mixpanel convention); Do tolerates it.
	if _, err := r.client.Do(ctx, "DELETE", r.instancePath(projectID, id), nil); err != nil {
		if apiErr, ok := err.(*client.APIError); ok && apiErr.StatusCode == 404 {
			return
		}
		resp.Diagnostics.AddError("Deleting rollup_project", err.Error())
		return
	}
}

func (r *RollupProjectResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	if req.ID == "" {
		resp.Diagnostics.AddError("Invalid import ID", "import ID must not be empty")
		return
	}
	setImportID(ctx, &resp.State, &resp.Diagnostics, "rollup_project_id", req.ID, "int64")
}

// writeRollupProjectState turns an unwrapped API body into resource state. base is the
// planned raw value (req.Plan.Raw) on create/update so config-supplied values are
// preserved verbatim, or a null tftypes.Value on read (state is rebuilt from the
// API response alone). See client.RawFromWireMerged for the merge semantics.
func (r *RollupProjectResource) writeRollupProjectState(ctx context.Context, state *tfsdk.State, diags *diagAppender, base tftypes.Value, wire map[string]any, projectID, id string) {
	extras := map[string]any{
		"rollup_project_id": id,
	}
	_ = projectID
	schemaType := state.Schema.Type().TerraformType(ctx)
	val, err := client.RawFromWireMerged(schemaType, base, wire, extras, RollupProjectAttrSpec())
	if err != nil {
		diags.AddError("Building rollup_project state", err.Error())
		return
	}
	state.Raw = val
}

// idForRollupProject extracts the identity value from an unwrapped response body.
func idForRollupProject(wire map[string]any) string {
	if v, ok := client.IDFromWire(wire, "rollup_project_id"); ok {
		return v
	}
	if v, ok := nestedID(wire, "id"); ok {
		return v
	}
	return ""
}

// unwrapRollupProject unwraps the API envelope (when enveloped) and returns the body map.
func unwrapRollupProject(respBody []byte) (map[string]any, error) {
	body, err := unwrapBody(respBody, true)
	if err != nil {
		return nil, err
	}
	return unwrapResultsMap(body, false), nil
}
