// Code generated by gen/crudgen.py. DO NOT EDIT.

package provider

import (
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/hashicorp/terraform-plugin-testing/plancheck"
)

func TestAccFormula_lifecycle(t *testing.T) {
	// The /metrics endpoints return enveloped single-entry id->object maps
	// (results_map) on create/get/update, verified against the live API.
	srv := newMockServer(t, mockOpts{enveloped: true, idField: "id", stringID: false, resultsMap: true, upsert: false, listCreate: false})

	resource.Test(t, resource.TestCase{
		ProtoV6ProviderFactories: testProtoV6,
		Steps: []resource.TestStep{
			{
				// Create; the implicit post-apply refresh+plan asserts idempotency.
				// The definition is a real FormulaMetricDefinition (formula
				// expression + referencedMetrics) accepted by the live API.
				Config: providerConfig(srv.URL, `
resource "mixpanel_formula" "test" {
  name = "tf-acc-test"
  type = "formula"
  definition = jsonencode({
    formula = {
      definition = "A"
      referencedMetrics = [{
        display = {}
        behavior = {
          name         = "Viewed report"
          type         = "event"
          search       = ""
          dataset      = "$mixpanel"
          filters      = []
          resourceType = "events"
        }
        measurement = {
          math       = "unique"
          cumulative = false
        }
      }]
    }
  })
  project_id = 1
}`),
			},
			{
				// A changed attribute must plan as the expected action.
				Config: providerConfig(srv.URL, `
resource "mixpanel_formula" "test" {
  name = "tf-acc-renamed"
  type = "formula"
  definition = jsonencode({
    formula = {
      definition = "A"
      referencedMetrics = [{
        display = {}
        behavior = {
          name         = "Viewed report"
          type         = "event"
          search       = ""
          dataset      = "$mixpanel"
          filters      = []
          resourceType = "events"
        }
        measurement = {
          math       = "unique"
          cumulative = false
        }
      }]
    }
  })
  project_id = 1
}`),
				ConfigPlanChecks: resource.ConfigPlanChecks{
					PreApply: []plancheck.PlanCheck{
						plancheck.ExpectResourceAction("mixpanel_formula.test", plancheck.ResourceActionUpdate),
					},
				},
			},
			{
				// Import the resource and assert state round-trips through Read.
				ResourceName:                         "mixpanel_formula.test",
				ImportState:                          true,
				ImportStateVerify:                    true,
				ImportStateVerifyIdentifierAttribute: "id",
				ImportStateIdFunc:                    importIDFunc("mixpanel_formula.test", "id", "project_id"),
				ImportStateVerifyIgnore:              []string{"allow_staff_override", "can_share", "can_update_basic", "can_update_restricted", "can_update_visibility", "can_view", "created", "created_by", "definition", "is_locked", "is_superadmin", "is_visible", "last_verified", "last_verified_by", "modified"},
			},
		},
	})
}
