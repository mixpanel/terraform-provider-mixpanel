// Code generated by terraform-plugin-framework-generator DO NOT EDIT.

package resource_cohort

import (
	"context"
	"fmt"
	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-framework/types/basetypes"
	"github.com/hashicorp/terraform-plugin-go/tftypes"
	"strings"

	"github.com/hashicorp/terraform-plugin-framework/resource/schema"
)

func CohortResourceSchema(ctx context.Context) schema.Schema {
	return schema.Schema{
		Attributes: map[string]schema.Attribute{
			"behaviors": schema.SingleNestedAttribute{
				Attributes: map[string]schema.Attribute{},
				CustomType: BehaviorsType{
					ObjectType: types.ObjectType{
						AttrTypes: BehaviorsValue{}.AttributeTypes(ctx),
					},
				},
				Optional: true,
				Computed: true,
			},
			"cohort_id": schema.Int64Attribute{
				Optional: true,
				Computed: true,
			},
			"data_group_id": schema.StringAttribute{
				Optional: true,
				Computed: true,
			},
			"description": schema.StringAttribute{
				Optional: true,
				Computed: true,
			},
			"id": schema.Int64Attribute{
				Computed: true,
			},
			"is_locked": schema.BoolAttribute{
				Optional: true,
				Computed: true,
			},
			"is_visible": schema.BoolAttribute{
				Optional: true,
				Computed: true,
			},
			"name": schema.StringAttribute{
				Optional: true,
				Computed: true,
			},
			"project_id": schema.Int64Attribute{
				Optional: true,
				Computed: true,
			},
			"selector": schema.SingleNestedAttribute{
				Attributes: map[string]schema.Attribute{},
				CustomType: SelectorType{
					ObjectType: types.ObjectType{
						AttrTypes: SelectorValue{}.AttributeTypes(ctx),
					},
				},
				Optional: true,
				Computed: true,
			},
		},
	}
}

type CohortModel struct {
	Behaviors   BehaviorsValue `tfsdk:"behaviors"`
	CohortId    types.Int64    `tfsdk:"cohort_id"`
	DataGroupId types.String   `tfsdk:"data_group_id"`
	Description types.String   `tfsdk:"description"`
	Id          types.Int64    `tfsdk:"id"`
	IsLocked    types.Bool     `tfsdk:"is_locked"`
	IsVisible   types.Bool     `tfsdk:"is_visible"`
	Name        types.String   `tfsdk:"name"`
	ProjectId   types.Int64    `tfsdk:"project_id"`
	Selector    SelectorValue  `tfsdk:"selector"`
}

var _ basetypes.ObjectTypable = BehaviorsType{}

type BehaviorsType struct {
	basetypes.ObjectType
}

func (t BehaviorsType) Equal(o attr.Type) bool {
	other, ok := o.(BehaviorsType)

	if !ok {
		return false
	}

	return t.ObjectType.Equal(other.ObjectType)
}

func (t BehaviorsType) String() string {
	return "BehaviorsType"
}

func (t BehaviorsType) ValueFromObject(ctx context.Context, in basetypes.ObjectValue) (basetypes.ObjectValuable, diag.Diagnostics) {
	var diags diag.Diagnostics

	if diags.HasError() {
		return nil, diags
	}

	return BehaviorsValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewBehaviorsValueNull() BehaviorsValue {
	return BehaviorsValue{
		state: attr.ValueStateNull,
	}
}

func NewBehaviorsValueUnknown() BehaviorsValue {
	return BehaviorsValue{
		state: attr.ValueStateUnknown,
	}
}

func NewBehaviorsValue(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) (BehaviorsValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	// Reference: https://github.com/hashicorp/terraform-plugin-framework/issues/521
	ctx := context.Background()

	for name, attributeType := range attributeTypes {
		attribute, ok := attributes[name]

		if !ok {
			diags.AddError(
				"Missing BehaviorsValue Attribute Value",
				"While creating a BehaviorsValue value, a missing attribute value was detected. "+
					"A BehaviorsValue must contain values for all attributes, even if null or unknown. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("BehaviorsValue Attribute Name (%s) Expected Type: %s", name, attributeType.String()),
			)

			continue
		}

		if !attributeType.Equal(attribute.Type(ctx)) {
			diags.AddError(
				"Invalid BehaviorsValue Attribute Type",
				"While creating a BehaviorsValue value, an invalid attribute value was detected. "+
					"A BehaviorsValue must use a matching attribute type for the value. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("BehaviorsValue Attribute Name (%s) Expected Type: %s\n", name, attributeType.String())+
					fmt.Sprintf("BehaviorsValue Attribute Name (%s) Given Type: %s", name, attribute.Type(ctx)),
			)
		}
	}

	for name := range attributes {
		_, ok := attributeTypes[name]

		if !ok {
			diags.AddError(
				"Extra BehaviorsValue Attribute Value",
				"While creating a BehaviorsValue value, an extra attribute value was detected. "+
					"A BehaviorsValue must not contain values beyond the expected attribute types. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("Extra BehaviorsValue Attribute Name: %s", name),
			)
		}
	}

	if diags.HasError() {
		return NewBehaviorsValueUnknown(), diags
	}

	if diags.HasError() {
		return NewBehaviorsValueUnknown(), diags
	}

	return BehaviorsValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewBehaviorsValueMust(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) BehaviorsValue {
	object, diags := NewBehaviorsValue(attributeTypes, attributes)

	if diags.HasError() {
		// This could potentially be added to the diag package.
		diagsStrings := make([]string, 0, len(diags))

		for _, diagnostic := range diags {
			diagsStrings = append(diagsStrings, fmt.Sprintf(
				"%s | %s | %s",
				diagnostic.Severity(),
				diagnostic.Summary(),
				diagnostic.Detail()))
		}

		panic("NewBehaviorsValueMust received error(s): " + strings.Join(diagsStrings, "\n"))
	}

	return object
}

func (t BehaviorsType) ValueFromTerraform(ctx context.Context, in tftypes.Value) (attr.Value, error) {
	if in.Type() == nil {
		return NewBehaviorsValueNull(), nil
	}

	if !in.Type().Equal(t.TerraformType(ctx)) {
		return nil, fmt.Errorf("expected %s, got %s", t.TerraformType(ctx), in.Type())
	}

	if !in.IsKnown() {
		return NewBehaviorsValueUnknown(), nil
	}

	if in.IsNull() {
		return NewBehaviorsValueNull(), nil
	}

	attributes := map[string]attr.Value{}

	val := map[string]tftypes.Value{}

	err := in.As(&val)

	if err != nil {
		return nil, err
	}

	for k, v := range val {
		a, err := t.AttrTypes[k].ValueFromTerraform(ctx, v)

		if err != nil {
			return nil, err
		}

		attributes[k] = a
	}

	return NewBehaviorsValueMust(BehaviorsValue{}.AttributeTypes(ctx), attributes), nil
}

func (t BehaviorsType) ValueType(ctx context.Context) attr.Value {
	return BehaviorsValue{}
}

var _ basetypes.ObjectValuable = BehaviorsValue{}

type BehaviorsValue struct {
	state attr.ValueState
}

func (v BehaviorsValue) ToTerraformValue(ctx context.Context) (tftypes.Value, error) {
	attrTypes := make(map[string]tftypes.Type, 0)

	objectType := tftypes.Object{AttributeTypes: attrTypes}

	switch v.state {
	case attr.ValueStateKnown:
		vals := make(map[string]tftypes.Value, 0)

		if err := tftypes.ValidateValue(objectType, vals); err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		return tftypes.NewValue(objectType, vals), nil
	case attr.ValueStateNull:
		return tftypes.NewValue(objectType, nil), nil
	case attr.ValueStateUnknown:
		return tftypes.NewValue(objectType, tftypes.UnknownValue), nil
	default:
		panic(fmt.Sprintf("unhandled Object state in ToTerraformValue: %s", v.state))
	}
}

func (v BehaviorsValue) IsNull() bool {
	return v.state == attr.ValueStateNull
}

func (v BehaviorsValue) IsUnknown() bool {
	return v.state == attr.ValueStateUnknown
}

func (v BehaviorsValue) String() string {
	return "BehaviorsValue"
}

func (v BehaviorsValue) ToObjectValue(ctx context.Context) (basetypes.ObjectValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	attributeTypes := map[string]attr.Type{}

	if v.IsNull() {
		return types.ObjectNull(attributeTypes), diags
	}

	if v.IsUnknown() {
		return types.ObjectUnknown(attributeTypes), diags
	}

	objVal, diags := types.ObjectValue(
		attributeTypes,
		map[string]attr.Value{})

	return objVal, diags
}

func (v BehaviorsValue) Equal(o attr.Value) bool {
	other, ok := o.(BehaviorsValue)

	if !ok {
		return false
	}

	if v.state != other.state {
		return false
	}

	if v.state != attr.ValueStateKnown {
		return true
	}

	return true
}

func (v BehaviorsValue) Type(ctx context.Context) attr.Type {
	return BehaviorsType{
		basetypes.ObjectType{
			AttrTypes: v.AttributeTypes(ctx),
		},
	}
}

func (v BehaviorsValue) AttributeTypes(ctx context.Context) map[string]attr.Type {
	return map[string]attr.Type{}
}

var _ basetypes.ObjectTypable = SelectorType{}

type SelectorType struct {
	basetypes.ObjectType
}

func (t SelectorType) Equal(o attr.Type) bool {
	other, ok := o.(SelectorType)

	if !ok {
		return false
	}

	return t.ObjectType.Equal(other.ObjectType)
}

func (t SelectorType) String() string {
	return "SelectorType"
}

func (t SelectorType) ValueFromObject(ctx context.Context, in basetypes.ObjectValue) (basetypes.ObjectValuable, diag.Diagnostics) {
	var diags diag.Diagnostics

	if diags.HasError() {
		return nil, diags
	}

	return SelectorValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewSelectorValueNull() SelectorValue {
	return SelectorValue{
		state: attr.ValueStateNull,
	}
}

func NewSelectorValueUnknown() SelectorValue {
	return SelectorValue{
		state: attr.ValueStateUnknown,
	}
}

func NewSelectorValue(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) (SelectorValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	// Reference: https://github.com/hashicorp/terraform-plugin-framework/issues/521
	ctx := context.Background()

	for name, attributeType := range attributeTypes {
		attribute, ok := attributes[name]

		if !ok {
			diags.AddError(
				"Missing SelectorValue Attribute Value",
				"While creating a SelectorValue value, a missing attribute value was detected. "+
					"A SelectorValue must contain values for all attributes, even if null or unknown. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("SelectorValue Attribute Name (%s) Expected Type: %s", name, attributeType.String()),
			)

			continue
		}

		if !attributeType.Equal(attribute.Type(ctx)) {
			diags.AddError(
				"Invalid SelectorValue Attribute Type",
				"While creating a SelectorValue value, an invalid attribute value was detected. "+
					"A SelectorValue must use a matching attribute type for the value. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("SelectorValue Attribute Name (%s) Expected Type: %s\n", name, attributeType.String())+
					fmt.Sprintf("SelectorValue Attribute Name (%s) Given Type: %s", name, attribute.Type(ctx)),
			)
		}
	}

	for name := range attributes {
		_, ok := attributeTypes[name]

		if !ok {
			diags.AddError(
				"Extra SelectorValue Attribute Value",
				"While creating a SelectorValue value, an extra attribute value was detected. "+
					"A SelectorValue must not contain values beyond the expected attribute types. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("Extra SelectorValue Attribute Name: %s", name),
			)
		}
	}

	if diags.HasError() {
		return NewSelectorValueUnknown(), diags
	}

	if diags.HasError() {
		return NewSelectorValueUnknown(), diags
	}

	return SelectorValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewSelectorValueMust(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) SelectorValue {
	object, diags := NewSelectorValue(attributeTypes, attributes)

	if diags.HasError() {
		// This could potentially be added to the diag package.
		diagsStrings := make([]string, 0, len(diags))

		for _, diagnostic := range diags {
			diagsStrings = append(diagsStrings, fmt.Sprintf(
				"%s | %s | %s",
				diagnostic.Severity(),
				diagnostic.Summary(),
				diagnostic.Detail()))
		}

		panic("NewSelectorValueMust received error(s): " + strings.Join(diagsStrings, "\n"))
	}

	return object
}

func (t SelectorType) ValueFromTerraform(ctx context.Context, in tftypes.Value) (attr.Value, error) {
	if in.Type() == nil {
		return NewSelectorValueNull(), nil
	}

	if !in.Type().Equal(t.TerraformType(ctx)) {
		return nil, fmt.Errorf("expected %s, got %s", t.TerraformType(ctx), in.Type())
	}

	if !in.IsKnown() {
		return NewSelectorValueUnknown(), nil
	}

	if in.IsNull() {
		return NewSelectorValueNull(), nil
	}

	attributes := map[string]attr.Value{}

	val := map[string]tftypes.Value{}

	err := in.As(&val)

	if err != nil {
		return nil, err
	}

	for k, v := range val {
		a, err := t.AttrTypes[k].ValueFromTerraform(ctx, v)

		if err != nil {
			return nil, err
		}

		attributes[k] = a
	}

	return NewSelectorValueMust(SelectorValue{}.AttributeTypes(ctx), attributes), nil
}

func (t SelectorType) ValueType(ctx context.Context) attr.Value {
	return SelectorValue{}
}

var _ basetypes.ObjectValuable = SelectorValue{}

type SelectorValue struct {
	state attr.ValueState
}

func (v SelectorValue) ToTerraformValue(ctx context.Context) (tftypes.Value, error) {
	attrTypes := make(map[string]tftypes.Type, 0)

	objectType := tftypes.Object{AttributeTypes: attrTypes}

	switch v.state {
	case attr.ValueStateKnown:
		vals := make(map[string]tftypes.Value, 0)

		if err := tftypes.ValidateValue(objectType, vals); err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		return tftypes.NewValue(objectType, vals), nil
	case attr.ValueStateNull:
		return tftypes.NewValue(objectType, nil), nil
	case attr.ValueStateUnknown:
		return tftypes.NewValue(objectType, tftypes.UnknownValue), nil
	default:
		panic(fmt.Sprintf("unhandled Object state in ToTerraformValue: %s", v.state))
	}
}

func (v SelectorValue) IsNull() bool {
	return v.state == attr.ValueStateNull
}

func (v SelectorValue) IsUnknown() bool {
	return v.state == attr.ValueStateUnknown
}

func (v SelectorValue) String() string {
	return "SelectorValue"
}

func (v SelectorValue) ToObjectValue(ctx context.Context) (basetypes.ObjectValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	attributeTypes := map[string]attr.Type{}

	if v.IsNull() {
		return types.ObjectNull(attributeTypes), diags
	}

	if v.IsUnknown() {
		return types.ObjectUnknown(attributeTypes), diags
	}

	objVal, diags := types.ObjectValue(
		attributeTypes,
		map[string]attr.Value{})

	return objVal, diags
}

func (v SelectorValue) Equal(o attr.Value) bool {
	other, ok := o.(SelectorValue)

	if !ok {
		return false
	}

	if v.state != other.state {
		return false
	}

	if v.state != attr.ValueStateKnown {
		return true
	}

	return true
}

func (v SelectorValue) Type(ctx context.Context) attr.Type {
	return SelectorType{
		basetypes.ObjectType{
			AttrTypes: v.AttributeTypes(ctx),
		},
	}
}

func (v SelectorValue) AttributeTypes(ctx context.Context) map[string]attr.Type {
	return map[string]attr.Type{}
}
